app = "elove"
primary_region = "sin"

[build]
  dockerfile = "apps/worker/Dockerfile"

[[vm]]
  memory = "2gb"
  cpu_kind = "shared"
  cpus = 2

[env]
  NODE_ENV = "production"

[processes]
  worker = "npx tsx src/index.ts"
